import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e88e5', // Azul
    },
    secondary: {
      main: '#f50057', // Rosa
    },
  },
});

export default theme;